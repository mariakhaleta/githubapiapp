<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authorizationFragment">
    <fragment
        android:id="@+id/authorizationFragment"
        android:name="com.example.headwaytestapp.authorization.AuthorizationFragment"
        android:label="MainFragment" >
        <action
            android:id="@+id/toShowRepositoryFragment"
            app:destination="@id/showRepositoryFragment" />
    </fragment>
    <fragment
        android:id="@+id/showRepositoryFragment"
        android:name="com.example.headwaytestapp.show_repos.ShowRepositoryFragment"
        android:label="ShowRepositoryFragment" >
    </fragment>
</navigation>